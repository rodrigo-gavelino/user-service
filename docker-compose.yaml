version: '3'

services:
  user-service:
    build: .
    command: ./.docker/start.sh
    ports:
      - '3000:3000'
    volumes:
      - .:/home/node/user-service
    depends_on:
      - mongodb

  mongodb:
    image: mongo:latest
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
